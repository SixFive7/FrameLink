TODO.md

# .NET build output
bin/
obj/
artifacts/

# Node / SvelteKit (Fleet Manager GUI)
node_modules/
.svelte-kit/

# Agent binaries staged for deployment by the harness
build/out/

# Harness run artifacts: screenshots, journal captures, console dumps.
# The resumable progress file is deliberately NOT ignored — it is the record that
# lets a fresh session pick up mid-milestone (version2.md §5.5).
tools/harness/runs/

# Python bytecode, written beside the harness sources the moment it is run from source
# (tools/harness/__pycache__ and tools/harness/flh/__pycache__).
__pycache__/

# Disk images and other large local archives. Never committed - the v1 card image
# alone is 119 GiB. Kept in the working directory for convenience, not for git.
archive/
